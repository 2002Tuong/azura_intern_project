package com.example.claptofindphone.models

import androidx.annotation.Keep

@Keep
data class OnBoardingPageData(val label: String, val subLabel: String, val imageResId: Int, val backgroundResId: Int)